1. Unix-like systems only
2. maven-surefire-getty
	-- install official maven (3.x.x) first
	-- deploy modified surefire plugin and read DEV-README to install
(3). Daikon
	-- put daikon.jar (v5.2.0) on $CLASSPATH
	-- put tools.jar (Java 7) on $CLASSPATH
4. simplediff
	-- `pip install simplediff`
(5). Randoop and JUnit 4 (and 3)
	-- put the following jar(s) on $CLASSPATH
		-- randoop-2.1.2.jar
(6). JUnit
	-- put JUnit (4.12-getty or revised) lib in front of all java classpaths
		-- getty-junit-4.12.jar (configured automatically by tool)
7. Deploy getty
